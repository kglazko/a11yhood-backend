# Test Environment Configuration
# Copy this to .env.test and fill in your test Supabase credentials

# Test Database Credentials (from test Supabase project)
SUPABASE_URL=https://your-test-project.supabase.co
SUPABASE_KEY=your-test-service-role-key
SUPABASE_ANON_KEY=your-test-anon-key

# CORS Configuration
# Security: Use strict origin allowlist; never use wildcards in production
# Dev: HTTPS Vite server with proxy (https://localhost:5173 -> http://localhost:8000)
# Prod: Set FRONTEND_URL and PRODUCTION_URL to actual domains (e.g., https://a11yhood.com)
FRONTEND_URL=https://localhost:5173
PRODUCTION_URL=

# Optional: Add extra allowed origins (comma-separated)
# CORS_EXTRA_ORIGINS=https://staging.example.com,https://preview.example.com

# Test-specific settings
TEST_MODE=true
TEST_SCRAPER_LIMIT=5

# GitHub OAuth (optional for tests, same as prod)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# GitHub API token for higher rate limits (optional but recommended)
GITHUB_TOKEN=

# Secret key for JWT (generate new random key for test environment)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-random-secret-key-for-testing
